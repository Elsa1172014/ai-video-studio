'use client';import Link from 'next/link';import {useEffect,useState} from 'react';import {loadProjects,removeProject,VideoProject} from '@/lib/project-store';export default function Projects(){const[p,setP]=useState<VideoProject[]>([]);useEffect(()=>setP(loadProjects()),[]);return <main className="mx-auto max-w-6xl p-6"><div className="flex items-center justify-between"><div><p className="text-sm text-purple-400">LIBRARY</p><h1 className="text-3xl font-bold">My video projects</h1></div><Link href="/" className="btn primary">+ New video</Link></div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{p.length?p.map(x=><div className="card p-5" key={x.id}><h2 className="font-bold">{x.title}</h2><p className="mt-2 line-clamp-2 text-sm text-slate-400">{x.prompt}</p><p className="mt-4 text-xs text-slate-500">{x.scenes.length} scenes · {x.minutes} min</p><button onClick={()=>{removeProject(x.id);setP(loadProjects())}} className="mt-4 text-xs text-red-400">Delete</button></div>):<div className="card p-8 text-slate-400">No projects yet. Create your first AI video.</div>}</div></main>