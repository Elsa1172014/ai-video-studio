import {NextResponse} from 'next/server';import {z} from 'zod';import {submitGeneration} from '@/lib/generation';const S=z.object({provider:z.string().default('wan'),prompt:z.string().min(3),duration:z.number().min(1).max(30),imageUrl:z.string().url().optional()});export async function POST(req:Request){try{return NextResponse.json(await submitGeneration(S.parse(await req.json())))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Generation failed'},{status:500})}}