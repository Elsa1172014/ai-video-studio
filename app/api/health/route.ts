import {NextResponse} from 'next/server';export async function GET(){const base=process.env.GPU_API_URL;if(!base)return NextResponse.json({web:true,gpu:false,status:'web-ready-gpu-unconfigured'});try{const r=await fetch(base.replace(/\/$/,'')+'/health',{cache:'no-store'});return NextResponse.json({web:true,gpu:r.ok,worker:r.ok?await r.json():null})}catch{return NextResponse.json({web:true,gpu:false,status:'gpu-unreachable'})}}