import {NextResponse} from 'next/server';export async function POST(req:Request){const base=process.env.GPU_API_URL;if(!base)return NextResponse.json({status:'unconfigured',message:'Connect GPU_API_URL to render MP4'});const r=await fetch(base.replace(/\/$/,'')+'/render',{method:'POST',headers:{'Content-Type':'application/json',...(process.env.GPU_API_KEY?{Authorization:`Bearer ${process.env.GPU_API_KEY}`}:{})},body:JSON.stringify(await req.json())});return NextResponse.json(await r.json(),{status:r.status})}