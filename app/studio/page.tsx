'use client';import {useState} from 'react';import MediaControls from '@/components/MediaControls';import Timeline from '@/components/Timeline';import ExportPanel from '@/components/ExportPanel';const stages=['Script','Scenes','Voice','Avatar','Lip Sync','Captions','Render'];export default function Studio(){const[active,setActive]=useState(0);return <main className="mx-auto max-w-7xl p-6"><p className="text-sm text-purple-400">PRODUCTION STUDIO</p><h1 className="text-3xl font-bold">Long Video Pipeline</h1><div className="mt-6 grid gap-3 md:grid-cols-7">{stages.map((x,i)=><button onClick={()=>setActive(i)} key={x} className={'rounded-xl border p-3 text-sm '+(active===i?'border-purple-500 bg-purple-500/15':'border-white/10')}>{i+1}. {x}</button>)}</div><div className="card mt-6 p-8"><h2 className="text-2xl font-bold">{stages[active]}</h2><p className="mt-3 text-slate-400">{active===0?'Generate or edit narration and dialogue.':active===1?'Break the story into coherent visual scenes.':active===2?'Generate narration or cloned voice audio.':active===3?'Animate a portrait/avatar with LivePortrait or EchoMimic.':active===4?'Synchronize speech and mouth movement with MuseTalk.':active===5?'Transcribe, align and style multilingual subtitles.':'Combine scenes, audio, captions and transitions into the final MP4.'}</p><div className="mt-6 rounded-xl border border-white/10 p-5"><MediaControls/></div><Timeline/><ExportPanel/></div></main>