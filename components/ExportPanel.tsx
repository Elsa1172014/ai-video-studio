'use client';import {useState} from 'react';export default function ExportPanel(){const[msg,setMsg]=useState('');async function render(){setMsg('Preparing render…');const r=await fetch('/api/render',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({clips:[]})});const j=await r.json();setMsg(j.message||j.error||j.outputUrl||j.status)}return <div className="card mt-6 p-5"><div className="flex items-center justify-between"><div><p className="text-xs text-purple-400">EXPORT</p><h2 className="font-bold">Final MP4</h2></div><button onClick={render} className="btn primary">Render video</button></div>{msg&&<p className="mt-3 text-sm text-slate-400">{msg}</p>}</div>