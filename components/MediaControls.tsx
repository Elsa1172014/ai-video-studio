'use client';import {useState} from 'react';export default function MediaControls(){const[tab,setTab]=useState('voice');const[text,setText]=useState('مرحباً بكم في استوديو الفيديو بالذكاء الاصطناعي.');const[msg,setMsg]=useState('');async function voice(){setMsg('Working…');const r=await fetch('/api/voice',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text,language:'ar',voice:'default'})});const j=await r.json();setMsg(j.message||j.status||'Submitted')}return <div><div className="flex gap-2"><button onClick={()=>setTab('voice')} className="btn ghost">Voice</button><button onClick={()=>setTab('avatar')} className="btn ghost">Avatar</button><button onClick={()=>setTab('dub')} className="btn ghost">Dubbing</button></div>{tab==='voice'&&<div className="mt-5"><textarea value={text} onChange={e=>setText(e.target.value)} className="min-h-28 w-full rounded-xl border border-white/10 bg-black/20 p-4" dir="auto"/><button onClick={voice} className="btn primary mt-3">Generate Arabic voice</button></div>}{tab==='avatar'&&<div className="mt-5 rounded-xl border border-dashed border-white/15 p-8"><b>Avatar source</b><p className="mt-2 text-sm text-slate-400">Image upload UI is ready; persistent media storage will be connected to object storage.</p><input type="file" accept="image/*" className="mt-4 block text-sm"/></div>}{tab==='dub'&&<div className="mt-5"><p className="text-sm text-slate-400">Arabic ↔ English dubbing pipeline: transcription → translation → TTS → timing.</p><input type="file" accept="video/*,audio/*" className="mt-4 block text-sm"/></div>}{msg&&<p className="mt-4 text-sm text-purple-300">{msg}</p>}</div>