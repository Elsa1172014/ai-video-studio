'use client';import {useState} from 'react';export default function GenerationPanel({prompt}:{prompt:string}){const[provider,setProvider]=useState('wan');const[job,setJob]=useState<any>(null);const[busy,setBusy]=useState(false);async function run(){setBusy(true);const r=await fetch('/api/generate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({provider,prompt,duration:5})});setJob(await r.json());setBusy(false)}return <div className="card mt-5 p-5"><div className="flex items-center justify-between gap-3"><div><p className="text-xs text-purple-400">GENERATION ENGINE</p><h2 className="font-bold">Generate selected scene</h2></div><select value={provider} onChange={e=>setProvider(e.target.value)} className="ghost rounded-xl p-3"><option value="wan">Wan 2.2</option><option value="ltx">LTX Video</option></select></div><button onClick={run} disabled={busy||!prompt} className="btn primary mt-4 w-full disabled:opacity-40">{busy?'Submitting…':'Generate video clip'}</button>{job&&<div className="mt-4 rounded-xl border border-white/10 p-3 text-sm"><b>Status: {job.status||'error'}</b><p className="mt-1 text-slate-400">{job.message||job.error||job.jobId}</p>{job.outputUrl&&<video className="mt-3 w-full rounded-xl" controls src={job.outputUrl}/>}</div>}</div>